"""
Legacy entry point — kept for backward compatibility with build.ps1 and LuminaControl.spec.
The actual application lives in the ``lumina_control`` package.
"""
from lumina_control.__main__ import main

main()
